apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "goserv.serviceAccountName" . }}
  labels:
    {{- include "goserv.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
