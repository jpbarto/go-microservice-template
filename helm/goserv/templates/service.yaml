apiVersion: v1
kind: Service
metadata:
  name: {{ include "goserv.fullname" . }}
  labels:
    {{- include "goserv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goserv.selectorLabels" . | nindent 4 }}
